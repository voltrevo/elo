import * as preact from 'preact';

type Props = {};
type State = {};

export default class Tour extends preact.Component<Props, State> {
  render(): preact.ComponentChild {
    this;

    return <div class="tour">
      Lorem ipsum
    </div>;
  }
}
